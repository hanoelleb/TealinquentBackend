<!DOCTYPE html>
<html>
    <head>
        <meta charset = 'UTF-8'></meta>
	<title><%= title %></title>
        <link rel='stylesheet' href='/stylesheets/style.css' />
    </head>
    <body>
        <%- include('navbar') %>
        <h1><%= title %></h1>
	<% if (products.length > 0) { %>
	    <p>Please delete/remove this category from the following products before attempting to delete this category.
	    </p>
	    <dl>
	    <% for (var i = 0; i < products.length; i++) { %>
	        <dt>
		<a href=<%= products[i].url %>>
		    <h3><%= products[i].name %></h3>
		</a> 
		</dt>
		<dd>
		  <h3>Product Description</h3>
		  <p><%= products[i].description %></p>
		  <hr>
                  <h3>Categories</h3>
		  <p> 
		   <% if (products[i].categories.length > 0) { %>
                    <% for (var j = 0; j < products[i].categories.length; j++) { %>
                      <p>
                        <a href= <%= products[i].categories[j].url%>>
                          <%= products[i].categories[j].name %>
                        </a>
                      </p>
                    <% } %>
                   <% } %>
		  </p>
		</dd>
	    <% } %>
	    </dl>
	<% } else { %>
        <form method='POST' action=''>
            <p>Do you really want to delete this category?</p>
            <input type='hidden' name='categoryId' value=<%= category._id %> ></input>
            <input type='submit' value='Delete'></input>
        </form>
	<% } %>
    </body>
</html>

